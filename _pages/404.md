---
layout: page
permalink: /404.html
title: "404: Lost in Space"
description: "This page doesn't exist, but let's find what you're looking for"
nav: false
---

<style>
header.post-header {
    display: none;
}
</style>
<div class="error-404-page">
  <div class="error-container">
    <div class="error-visual">
      <div class="error-code">404</div>
      <div class="error-icon">
        <i class="fa-solid fa-rocket"></i>
      </div>
    </div>

    <div class="error-content">
      <h1 class="error-title">Oops! You've ventured into uncharted territory</h1>
      <p class="error-description">
        The page you're looking for has either been moved, deleted, or never existed in the first place.
        But don't worry, even the best explorers get lost sometimes.
      </p>

      <div class="error-search">
        <h3>What are you looking for?</h3>
        <div class="search-suggestions">
          <a href="{{ '/' | relative_url }}" class="suggestion-btn">
            <i class="fa-solid fa-home"></i>
            Home
          </a>
          <a href="{{ '/blog/' | relative_url }}" class="suggestion-btn">
            <i class="fa-solid fa-blog"></i>
            Blog
          </a>
          <a href="{{ '/projects/' | relative_url }}" class="suggestion-btn">
            <i class="fa-solid fa-folder-open"></i>
            Projects
          </a>
          <a href="{{ '/publications/' | relative_url }}" class="suggestion-btn">
            <i class="fa-solid fa-book"></i>
            Publications
          </a>
          <a href="{{ '/cv/' | relative_url }}" class="suggestion-btn">
            <i class="fa-solid fa-file-alt"></i>
            CV
          </a>
          <a href="{{ '/about/' | relative_url }}" class="suggestion-btn">
            <i class="fa-solid fa-user"></i>
            About
          </a>
        </div>
      </div>

      <div class="error-help">
        <p class="help-text">
          <i class="fa-solid fa-lightbulb"></i>
          <strong>Pro tip:</strong> Use the search function (press <kbd>/</kbd>) to quickly find what you're looking for.
        </p>
      </div>

      <div class="error-action">
        <a href="javascript:history.back()" class="btn-back">
          <i class="fa-solid fa-arrow-left"></i>
          Go Back
        </a>
        <a href="{{ '/' | relative_url }}" class="btn-home">
          <i class="fa-solid fa-home"></i>
          Take Me Home
        </a>
      </div>
    </div>

  </div>

  <div class="error-footer">
    <p>Error Code: 404 | Lost but not forgotten</p>
  </div>
</div>

<style>
.error-404-page {
  min-height: 70vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2rem 1rem;
  text-align: center;
}

.error-container {
  max-width: 800px;
  margin: 0 auto;
}

.error-visual {
  position: relative;
  margin-bottom: 3rem;
}

.error-code {
  font-size: clamp(6rem, 15vw, 12rem);
  font-weight: 900;
  color: var(--global-theme-color);
  opacity: 0.1;
  line-height: 1;
  letter-spacing: -0.05em;
  user-select: none;
}

.error-icon {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-45deg);
  font-size: 5rem;
  color: var(--global-theme-color);
  animation: float 3s ease-in-out infinite;
}

@keyframes float {
  0%, 100% {
    transform: translate(-50%, -50%) rotate(-45deg) translateY(0px);
  }
  50% {
    transform: translate(-50%, -50%) rotate(-45deg) translateY(-20px);
  }
}

.error-content {
  margin-bottom: 3rem;
}

.error-title {
  font-size: clamp(1.75rem, 4vw, 2.5rem);
  font-weight: 800;
  color: var(--global-text-color);
  margin-bottom: 1rem;
  letter-spacing: -0.02em;
}

.error-description {
  font-size: 1.125rem;
  line-height: 1.7;
  color: var(--global-text-color-light);
  margin-bottom: 3rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.error-search {
  margin: 3rem 0;
  padding: 2rem;
  background: var(--global-code-bg-color);
  border-radius: 16px;
  border: 1px solid var(--global-divider-color);
}

.error-search h3 {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--global-text-color);
  margin-bottom: 1.5rem;
}

.search-suggestions {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap: 1rem;
  max-width: 600px;
  margin: 0 auto;
}

.suggestion-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem;
  background: var(--global-bg-color);
  border: 1px solid var(--global-divider-color);
  border-radius: 12px;
  color: var(--global-text-color);
  text-decoration: none;
  font-weight: 600;
  font-size: 0.9rem;
  transition: all 0.3s ease;
}

.suggestion-btn i {
  font-size: 1.5rem;
  color: var(--global-theme-color);
}

.suggestion-btn:hover {
  transform: translateY(-4px);
  border-color: var(--global-theme-color);
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
  color: var(--global-text-color);
}

.error-help {
  margin: 2rem 0;
  padding: 1.25rem;
  background: var(--global-code-bg-color);
  border-left: 4px solid var(--global-theme-color);
  border-radius: 8px;
  text-align: left;
}

.help-text {
  margin: 0;
  font-size: 0.95rem;
  line-height: 1.6;
  color: var(--global-text-color);
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
}

.help-text i {
  color: var(--global-theme-color);
  margin-top: 0.2rem;
}

.help-text kbd {
  padding: 0.2rem 0.5rem;
  background: var(--global-bg-color);
  border: 1px solid var(--global-divider-color);
  border-radius: 4px;
  font-family: "Fira Code", monospace;
  font-size: 0.85rem;
  font-weight: 600;
}

.error-action {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
  margin-top: 2rem;
}

.btn-back,
.btn-home {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.875rem 1.75rem;
  border-radius: 12px;
  font-weight: 700;
  text-decoration: none;
  transition: all 0.3s ease;
  font-size: 1rem;
}

.btn-back {
  background: transparent;
  color: var(--global-text-color);
  border: 2px solid var(--global-divider-color);
}

.btn-back:hover {
  border-color: var(--global-theme-color);
  color: var(--global-theme-color);
  transform: translateX(-4px);
}

.btn-home {
  background: var(--global-theme-color);
  color: white;
  border: 2px solid var(--global-theme-color);
}

.btn-home:hover {
  background: transparent;
  color: var(--global-theme-color);
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
}

.error-footer {
  margin-top: 4rem;
  padding-top: 2rem;
  border-top: 1px solid var(--global-divider-color);
}

.error-footer p {
  font-size: 0.875rem;
  color: var(--global-text-color-light);
  margin: 0;
  font-family: "Fira Code", monospace;
}

/* Dark mode adjustments */
[data-theme='dark'] .suggestion-btn:hover,
html.dark-mode .suggestion-btn:hover {
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
}

/* Responsive */
@media (max-width: 768px) {
  .error-404-page {
    padding: 1rem;
    min-height: 60vh;
  }

  .error-code {
    font-size: 5rem;
  }

  .error-icon {
    font-size: 3rem;
  }

  .error-title {
    font-size: 1.5rem;
  }

  .error-description {
    font-size: 1rem;
  }

  .search-suggestions {
    grid-template-columns: repeat(2, 1fr);
  }

  .error-action {
    flex-direction: column;
  }

  .btn-back,
  .btn-home {
    width: 100%;
    justify-content: center;
  }

  .help-text {
    font-size: 0.875rem;
    flex-direction: column;
    gap: 0.5rem;
  }
}

@media (max-width: 480px) {
  .search-suggestions {
    grid-template-columns: 1fr;
  }

  .suggestion-btn {
    flex-direction: row;
    justify-content: flex-start;
  }
}
</style>
